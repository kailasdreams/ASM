<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>F5 ASM Summary</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .text-blocking { color: red; font-weight: bold; }
    .text-transparent { color: orange; font-weight: bold; }
    .text-total { color: blue; font-weight: bold; }
    .text-header { color: #0d6efd; font-weight: 600; }
  </style>
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2 class="mb-4 text-header">F5 ASM Device Summary</h2>
    <table class="table table-bordered table-striped text-center align-middle">
      <thead class="table-dark">
        <tr>
          <th>F5 IP</th>
          <th>Hostname</th>
          <th>Total Policies</th>
          <th>Blocking</th>
          <th>Transparent</th>
        </tr>
      </thead>
      <tbody>
        {% for device in devices %}
        <tr>
          <td><a href="{{ url_for('policy_details', ip=device.ip) }}">{{ device.ip }}</a></td>
          <td>{{ device.hostname }}</td>
          <td class="text-total">{{ device.total }}</td>
          <td class="text-blocking">{{ device.blocking }}</td>
          <td class="text-transparent">{{ device.transparent }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</body>
</html>
