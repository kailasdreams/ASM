<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>F5 ASM Summary</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .bg-blocking {
      background-color: #f8d7da !important;
    }
    .bg-transparent {
      background-color: #fff3cd !important;
    }
    .bg-total {
      background-color: #cfe2ff !important;
    }
    td a {
      color: #0d6efd;
      text-decoration: none;
    }
    td a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body class="bg-light">
<div class="container mt-5">
  <h2 class="mb-4 text-primary text-center">🔐 F5 ASM Device Summary</h2>
  <table class="table table-bordered table-hover">
    <thead class="table-dark text-center">
      <tr>
        <th>F5 IP</th>
        <th>Hostname</th>
        <th class="bg-total">Total Policies</th>
        <th class="bg-blocking">Blocking</th>
        <th class="bg-transparent">Transparent</th>
      </tr>
    </thead>
    <tbody class="text-center">
      {% for device in devices %}
        <tr>
          <td><a href="{{ url_for('policy_details', ip=device.ip) }}">{{ device.ip }}</a></td>
          <td>{{ device.hostname }}</td>
          <td class="bg-total">{{ device.total }}</td>
          <td class="bg-blocking">{{ device.blocking }}</td>
          <td class="bg-transparent">{{ device.transparent }}</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
</div>
</body>
</html>
