<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Policy Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .text-mode-blocking {
      color: red;
      font-weight: bold;
    }
    .text-mode-transparent {
      color: orange;
      font-weight: bold;
    }
    .table th, .table td {
      text-align: center;
      vertical-align: middle;
    }
    .table-striped > tbody > tr:nth-of-type(odd) {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h2 class="mb-4 text-center text-primary">🔍 F5 ASM Policy Details</h2>
    <table class="table table-bordered table-striped table-hover">
      <thead class="table-dark">
        <tr>
          <th>Device Name</th>
          <th>Device IP</th>
          <th>Policy Name</th>
          <th>Enforcement Mode</th>
          <th>VIP Name</th>
          <th>VIP Partition</th>
          <th>VIP Address</th>
        </tr>
      </thead>
      <tbody>
        {% for policy in policies %}
          {% for vip in policy.vips %}
            <tr>
              <td>{{ policy.device_name }}</td>
              <td>{{ policy.device_ip }}</td>
              <td>{{ policy.name }}</td>
              <td class="{% if policy.mode == 'blocking' %}text-mode-blocking{% elif policy.mode == 'transparent' %}text-mode-transparent{% endif %}">
                {{ policy.mode }}
              </td>
              <td>{{ vip.name }}</td>
              <td>{{ vip.partition }}</td>
              <td>{{ vip.address }}</td>
            </tr>
          {% endfor %}
        {% endfor %}
      </tbody>
    </table>
    <div class="text-center">
      <a href="{{ url_for('index') }}" class="btn btn-secondary mt-4">⬅️ Back to Summary</a>
    </div>
  </div>
</body>
</html>
