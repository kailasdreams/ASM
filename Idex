<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Policy Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .text-blocking { color: red; font-weight: bold; }
    .text-transparent { color: orange; font-weight: bold; }
    .custom-table {
      border: 2px solid #343a40 !important; /* Dark border for the table */
    }
    .custom-table th,
    .custom-table td {
      border: 1px solid #6c757d !important; /* Light border for cells */
    }
    .custom-thead {
      background-color: #007bff !important; /* Blue background for header */
      color: white !important;
    }
  </style>
</head>
<body class="bg-light p-4">
  <div class="container">
    <h2 class="mb-4 text-primary">ASM Policy & VIP Bindings</h2>
    <table class="table table-hover text-center align-middle custom-table">
      <thead class="custom-thead">
        <tr>
          <th>Device Name</th>
          <th>Device IP</th>
          <th>Policy Name</th>
          <th>Enforcement Mode</th>
          <th>VIP Name</th>
          <th>VIP Partition</th>
          <th>VIP Address</th>
        </tr>
      </thead>
      <tbody>
        {% for policy in policies %}
          {% for vip in policy.vips %}
            <tr>
              <td>{{ policy.device_name }}</td>
              <td>{{ policy.device_ip }}</td>
              <td>{{ policy.name }}</td>
              <td class="{% if policy.mode == 'blocking' %}text-blocking{% elif policy.mode == 'transparent' %}text-transparent{% endif %}">{{ policy.mode }}</td>
              <td>{{ vip.name }}</td>
              <td>{{ vip.partition }}</td>
              <td>{{ vip.address }}</td>
            </tr>
          {% endfor %}
        {% endfor %}
      </tbody>
    </table>
    <div class="text-end">
      <a href="{{ url_for('index') }}" class="btn btn-secondary mt-3">⬅ Back to Summary</a>
    </div>
  </div>
</body>
</html>
