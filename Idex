<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Enhanced F5 ASM Summary</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .table-container {
      margin-top: 30px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05); /* Subtle shadow for depth */
      border-radius: 8px; /* Slightly rounded corners */
      overflow: hidden; /* Ensures rounded corners work properly with the table */
    }

    .table th {
      background-color: #343a40; /* Dark background for header */
      color: white;
      font-weight: bold;
      text-align: center;
      padding: 12px;
    }

    .table td {
      padding: 10px;
      text-align: center;
      border-bottom: 1px solid #dee2e6; /* Light gray separator */
    }

    .table tbody tr:nth-child(even) {
      background-color: #f8f9fa; /* Light gray for even rows */
    }

    .table tbody tr:hover {
      background-color: #e9ecef; /* Slightly darker gray on hover */
    }

    .text-blocking {
      color: #dc3545; /* Red for blocking */
      font-weight: bold;
    }

    .text-transparent {
      color: #ffc107; /* Amber/Yellow for transparent */
      font-weight: bold;
    }

    .text-total {
      color: #007bff; /* Blue for total */
      font-weight: bold;
    }

    .table td a {
      color: #0d6efd;
      text-decoration: none;
    }

    .table td a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body class="bg-light">
<div class="container mt-5">
  <h2 class="mb-4 text-center text-primary">🛡️ Enhanced F5 ASM Device Summary</h2>
  <div class="table-container">
    <table class="table table-bordered table-hover">
      <thead class="table-dark">
        <tr>
          <th>F5 IP Address</th>
          <th>Hostname</th>
          <th>Total Policies</th>
          <th>Blocking Policies</th>
          <th>Transparent Policies</th>
        </tr>
      </thead>
      <tbody>
        {% for device in devices %}
          <tr>
            <td><a href="{{ url_for('policy_details', ip=device.ip) }}">{{ device.ip }}</a></td>
            <td>{{ device.hostname }}</td>
            <td class="text-total">{{ device.total }}</td>
            <td class="text-blocking">{{ device.blocking }}</td>
            <td class="text-transparent">{{ device.transparent }}</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
</body>
</html>
